# macOS
.DS_Store

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/

# XcodeGen generated project は方針としてコミットする（clone後に xcodegen 不要にするため）。
# ただしユーザー固有データ（scheme のユーザー設定等）は除外済み。
